Node.js backend for signaling with Socket.IO.
Expose env var PORT for Render. In Render create a Web Service with this repo root and set start command `npm start` in backend/service settings or use Dockerfile provided.
